import React, { Component } from 'react';
import { View, Text } from 'react-native';


const CarDetail = (key) => {
    return (
    <View>
        <Text>{key}</Text>
    </View>
    );
};


export default CarDetail;

